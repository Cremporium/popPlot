package setup;
import java.util.Scanner;

public class Setup {
	
	private Scanner scan = new Scanner(System.in);
	
	public void choice() {
		final int ch = scan.nextInt();
		
		if(ch == 1) {
			//Enter pop counter
		}
		else if(ch == 2) {
			//enter double time
		}
	}
}
